# Base URL for the API (production Worker URL)
# In development, this defaults to http://localhost:8787
# In production, set this to your deployed Worker URL
VITE_API_BASE_URL=https://your-worker-subdomain.your-account.workers.dev
